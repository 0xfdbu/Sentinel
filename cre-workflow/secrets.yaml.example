# Secrets Configuration for Sentinel CRE Contract Scanner
# 
# IMPORTANT: 
# - This file is used by the CRE CLI during simulation
# - For production, secrets are stored in the Vault DON
# - Never commit actual secrets to version control!
#
# The Sentinel project uses the root-level secrets.yaml for Vault DON mapping.
# This file follows the same structure for local simulation.

secretsNames:
  # Etherscan API key - injected via template {{.etherscanApiKey}}
  # Get your API key from: https://etherscan.io/apis
  etherscanApiKey:
    - ETHERSCAN_API_KEY

  # xAI Grok API key for AI-powered security analysis
  # Get your API key from: https://console.x.ai
  grokApiKey:
    - GROK_API_KEY

  # AES-256-GCM encryption key for Confidential HTTP (optional)
  # Generate with: openssl rand -hex 32
  san_marino_aes_gcm_encryption_key:
    - AES_KEY_ALL
