## Important note

Custom Event Listener auto-executes CRE CLI commands upon detecting on-chain events, eliminating the need for manual CLI input. This simulates production EVM Log Trigger behavior without requiring live Chainlink DON deployment.