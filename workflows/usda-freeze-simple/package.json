{
  "name": "usda-freeze-simple",
  "version": "1.0.0",
  "description": "Simple USDA address freeze via CRE workflow",
  "scripts": {
    "compile": "cre-compile",
    "simulate": "cre workflow simulate . --target local-simulation",
    "deploy": "cre workflow deploy . --target production"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.0.0",
    "viem": "^1.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
