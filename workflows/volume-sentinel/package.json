{
  "name": "volume-sentinel",
  "version": "1.0.0",
  "description": "ACE Volume Limit Adjustment via News + Sentiment + xAI Grok",
  "type": "module",
  "scripts": {
    "compile": "cre-compile",
    "simulate": "cre workflow simulate volume-sentinel --target local-simulation",
    "deploy": "cre workflow deploy volume-sentinel --target production"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.0.0",
    "viem": "^1.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
