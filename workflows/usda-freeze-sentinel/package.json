{
  "name": "usda-freeze-sentinel",
  "version": "1.0.0",
  "description": "USDA Freeze Sentinel - Monitors transfers and freezes scammer addresses via AI",
  "scripts": {
    "compile": "cre-compile",
    "simulate": "cre workflow simulate . --target local-simulation",
    "deploy": "cre workflow deploy . --target production"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.0.0",
    "viem": "^1.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
