{
  "name": "freeze-test",
  "version": "1.0.0",
  "description": "Simplified freeze test workflow",
  "type": "module",
  "scripts": {
    "compile": "cre-compile",
    "simulate": "cre workflow simulate freeze-test --target local-simulation",
    "deploy": "cre workflow deploy freeze-test --target production"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.0.0",
    "viem": "^1.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
