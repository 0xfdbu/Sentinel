{
  "name": "blacklist-manager",
  "version": "1.0.0",
  "description": "Blacklist Manager - Multi-source blacklist sync for ACE",
  "type": "module",
  "scripts": {
    "compile": "cre-compile",
    "simulate": "cre workflow simulate blacklist-manager --target local-simulation",
    "deploy": "cre workflow deploy blacklist-manager --target production"
  },
  "dependencies": {
    "@chainlink/cre-sdk": "^1.0.0",
    "viem": "^1.0.0",
    "zod": "^3.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
